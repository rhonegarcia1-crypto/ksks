import React from 'react';
import './AuthLoginForm.css';

const AuthLoginForm = () => {
    return (
        <div className="auth-login-form" style={{ background: 'linear-gradient(to right, #0000FF, #00BFFF)' }}>
            {/* Keep other form elements here */}
            <h1>Login</h1>
            {/* Other components remain unchanged */}
        </div>
    );
};

export default AuthLoginForm;